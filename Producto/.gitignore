# Claude Code / Agent skills (project-level, no subir al repo)
.claude
.agents
skills-lock.json

# node_modules residual del root anterior
node_modules
dist
dist-ssr

# Variables de entorno con credenciales (NO subir). Solo se versiona .env.example
.env
